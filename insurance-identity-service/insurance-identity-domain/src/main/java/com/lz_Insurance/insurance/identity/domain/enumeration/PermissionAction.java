package com.lz_Insurance.insurance.identity.domain.enumeration;

/**
 * The action half of a {@code resource:action:dataScope} permission.
 */
public enum PermissionAction {
    CREATE,
    READ,
    UPDATE,
    DELETE,
    APPROVE,
    REJECT,
    EXPORT,
    CONFIGURE
}
